interface:
  display_name: "WorkorAI"
  short_description: "Use WorkorAI talent marketplace MCP tools for candidate and employer workflows"
  brand_color: "#0F766E"
  default_prompt: "Use WorkorAI MCP for talent marketplace workflows. For candidates, find matched jobs and guide onboarding plus MCP key setup. For employers, run hire flows (search candidates, invite, review applicants, manage jobs) against the live WorkorAI employer MCP surface."

policy:
  allow_implicit_invocation: true
